# Overrides para o profile 'dev' (comuns a todos os serviços)
logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.springframework.jdbc: INFO

management:
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
