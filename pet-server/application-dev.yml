server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petshop_pet?useSSL=false&serverTimezone=UTC
    username: dev
    password: dev
  data:
    redis:
      host: localhost
      port: 6379
